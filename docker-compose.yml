services:
  latex-compiler:
    container_name: cheetah-latex-compiler
    build:
      context: ./services/latex-compiler
      dockerfile: Dockerfile
    ports:
      - '8080:8080'
    environment:
      HOST: 0.0.0.0
      PORT: 8080
